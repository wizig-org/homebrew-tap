# Homebrew Tap for Wizig

Install [Wizig](https://wizig.org) via Homebrew:

```sh
brew install wizig-org/tap/wizig
```

This formula is auto-updated by the [wizig release workflow](https://github.com/wizig-org/wizig).
